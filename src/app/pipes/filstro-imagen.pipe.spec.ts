import { FilstroImagenPipe } from './filstro-imagen.pipe';

describe('FilstroImagenPipe', () => {
  it('create an instance', () => {
    const pipe = new FilstroImagenPipe();
    expect(pipe).toBeTruthy();
  });
});
